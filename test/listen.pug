extends test-base

block title
	title listen.js test

block append style
	style
		include:sass query.sass

block append body
	script(type='module').
		import * as main from './listen-test.js'
		Object.assign(window, main)
